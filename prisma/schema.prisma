
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Driver {
  id  String @id @default(uuid())
  name String
  birth_date DateTime
  license String
  car_model String
  license_plate String

  @@map("drivers")

}
